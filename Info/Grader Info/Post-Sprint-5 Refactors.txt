-----Done-----:

-----Done > Sprint 5 early comments-----:

StateGameType - Santosh

-----Done > Sprint 5-----:

Controllers - Santosh
Seems like most controllers have similar functionality so an abstract class could clean up code, but for only three classes of relatively small size, it isn't that important

Commands - Santosh
Move add folders for the commands. Right now its pretty hard to find what command you're looking for as there's a bunch in the same file level without any organization

StateGameType (High Priority) - Santosh
Class is too large with some disconnected elements like key bindings
The LowLevelDraw function is far too large and contains granular details like individual sprite batch groups that should be moved out into easier to read functions
The class coupling for this class is 75 which is enormous and would be nightmare during testing

-----Not Done-----:

-----Not Done > Sprint 3-----:

Game1
Should not be passing around the main game often. Leads to high coupling
Seems to be better. Current implementation such that smaller classes get the LevelManager passed in, from which they have access to public methods that allow them to communicate with the game

LevelManager
What's the point of newLevelName? Why not just immediately switch the currentLevelName in SwitchLevel? Could be missing something
I still don't understand why there's a check to see if the current level is different to the level to fade to, as this seems like a violation of programming by contract, that is if a devoper wants a level that infinitely loops or something they wouldn't be able to accomplish it with this system. Then again I haven't been working on the project so there might be some weird bugs that arise

LevelLoader (High Priority; Excused)
High cyclomatic complexity with conditionals inside of switch statements inside of conditionals.
High cyclomatic complexity still, but cleaner and more or less excusable due to the nature of map loading

LevelLoader (High Priority; Excused)
Loops too nested
Largely fixed. Still has some conditionals inside switch statements inside switch statements. Hard to maintain, but excusable as mentioned, I'd imagine this system is fairly bespoke

LevelLoader (High Priority)
Maybe instead of using a massive switch statement, find some way to programmatically pick the required factory function, given that the contents of the switch statements are more or less the same
Not implemented

Level
Look at using coroutines instead of juggling the fade-in/out states. The C# keyword should be await. Even if you don't use coroutines the screen transitions should probably be separate from the level itself
Coroutines not implemented, but screen transitions have been moved to their own class, which is good

Player (High Priority)
Reduce the number of public fields. 
Still lots of public fields that are manipulated by lots of other classes 

Player (High Priority)
The player is tightly coupled with basically everything in the game. If I tried to copy and paste the Player class into another project I'd have to also copy like 5 other files for it just to compile
Coupling is mostly resolved to the levelManager instead of more specific classes

Player
The player shouldn't be drawing and updating the inventory. Player should just draw and update the player
Player still draws and updates the inventory

OpenSlattedDoor
A simple door shouldn't need to know about the game. 
Class replaced with SlattedDoorBlock class and replaces the game1 with level manager.

BaseEnemy (Medium Priority)
Lots of public fields, along with usual problem of passing in the game
Replaced with ABaseEnemy. Still contains a lot of public fields which are referenced a lot

-----Not Done > Sprint 5 early comments-----:

StateItemScreenType
Level
LevelLoader (excused based on being a level loading class)

-----Not Done > Sprint 5-----:

Game1 (Medium Priority)
All the states should not be contained in Game1, as it defeats the purpose of using states to obscure the general logic in the first place

Level
There's a lot of helper functions in Level that could moved out to make Level itself more coheasive

CollisionManager
DebugDraw doesn't belong in the collision manager and is never being referenced anyways

Player
Moves the states into their own state machine or something else instead of containing them all inside of the Player class. Adds unnecessary clutter

PlayerAnimatedMovingState
Lots of magic numbers.

BossAttackState
Magic numbers
